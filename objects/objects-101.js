let myBook = {
    title: '1984',
    author: 'George Orwell',
    pageCount: 326
}

console.log(`${myBook.title} by ${myBook.author}`)

myBook.title = 'Animal Farm'

console.log(`${myBook.title} by ${myBook.author}`)

// Challenge area

// Include name, age, location - object
let person = {
    name: 'Pontus',
    age: 26,
    location: 'Gothenburg'
}

// print it out
console.log(`${person.name} is ${person.age} and lives in ${person.location}`)

// increase age by 1 and print
person.age += 1;
console.log(`${person.name} is ${person.age} and lives in ${person.location}`)




